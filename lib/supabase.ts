// Alias agar dua jalur impor yang sudah dipakai di kode ('@/lib/supabase' dan
// '@/lib/supabaseClient') menunjuk ke satu instance client yang sama.
export { supabase, default } from './supabaseClient';
